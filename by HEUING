local Rayfield = loadstring(game:HttpGet('https://[Log in to view URL]'))()

local Window = Rayfield:CreateWindow({
   Name = "🎀HEUING HUB V1🎀",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "환영합니다!",
   LoadingSubtitle = "by HEUING",
   ShowText = "Rayfield", -- for mobile users to unhide rayfield, change if you'd like
   Theme = "Default", -- Check https://[Log in to view URL]

   ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = false,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "🎀HEUING HUB V1🎀",
      Subtitle = "HEUING HUB KEY",
      Note = "스크립트와 함께 키를 공유해놨습니다.", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = false, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"HEUING"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local MainTab = Window:CreateTab("💥Universal💥", nil) -- Title, Image
local MainSection = MainTab:CreateSection("Universal")

Rayfield:Notify({
   Title = "스크립트 로딩이 완료됐습니다.",
   Content = "즐거운 핵 쓰세요!",
   Duration = 6.5,
   Image = nil,
})

local Button = MainTab:CreateButton({
   Name = "💥Infinite yield💥",
   Callback = function()
   loadstring(game:HttpGet('https://[Log in to view URL]'))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💥Vape V4💥",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]", true))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💥Filng💥",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💥fly gui v3💥",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💥월홉(키:Q)💥",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local MainTab = Window:CreateTab("🎮Steal a brainrot🎮", nil) -- Title, Image
local MainSection = MainTab:CreateSection("Steal a brainrot")

local Button = MainTab:CreateButton({
   Name = "🎮OMG hub🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮Lemon Hub🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮Ronix hub🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮HoHo Hub🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮Makal Hub🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]",true))()
   end,
})

local MainTab = Window:CreateTab("🎮INK game🎮", nil) -- Title, Image
local MainSection = MainTab:CreateSection("INK game")

local Button = MainTab:CreateButton({
   Name = "🎮RIFT🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮siff🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮NEOX HUB🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮Tesla Hub🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮RINGTA🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮TUFF GUY🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "🎮shaq16🎮",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
       Name = "🎮eF Hub🎮",
   Callback = function()
   loadstring(game:HttpGet('https://[Log in to view URL]', true))()
   end,
})

local MainTab = Window:CreateTab("🎮Hypershot🎮", nil) -- Title, Image
local MainSection = MainTab:CreateSection("Hypershot")

local Button = MainTab:CreateButton({
   Name = "🎮Bring All🎮",
   Callback = function()
   local run_serivce = game:GetService("RunService")
local workspace = game:GetService("Workspace")
local players = game:GetService("Players")

local local_player = players.LocalPlayer
local camera = workspace.CurrentCamera

local bots = workspace:FindFirstChild("Mobs")
if (not bots) then
    return local_player:Kick("Real players not found!!!!!!!")
end

run_serivce.RenderStepped:Connect(function()
    for _, v in bots:GetChildren() do
        if (local_player.Character and local_player.Character:GetAttribute("Team") ~= -1) and (v:GetAttribute("Team") == local_player.Character:GetAttribute("Team")) then
            continue
        end
        local head = v:FindFirstChild("Head")
        if (not head) then
            continue
        end
        head.CFrame = camera.CFrame + camera.CFrame.lookVector * 7
    end

    for _, v in players:GetPlayers() do
        if (v == local_player) then
            continue
        end
        if (local_player.Character and local_player.Character:GetAttribute("Team") ~= -1) and (v.Character and v.Character:GetAttribute("Team") == local_player.Character:GetAttribute("Team")) then
            continue
        end
        local head = v.Character and v.Character:FindFirstChild("Head")
        if (not head) then
            continue
        end
        head.CFrame = camera.CFrame + camera.CFrame.lookVector * 7
    end
end)
   end,
})

local Button = MainTab:CreateButton({
       Name = "🎮Syrex Hub🎮",
   Callback = function()
   loadstring(game:HttpGet('https://[Log in to view URL]'))()
   end,
})

local MainTab = Window:CreateTab("💎HUB💎", nil) -- Title, Image
local MainSection = MainTab:CreateSection("HUB")

local Button = MainTab:CreateButton({
   Name = "💎BIOM HUB💎",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💎로블 핵 전문 HUB💎 키:팬분들사랑합니다",
   Callback = function()
   loadstring(game:HttpGet('https://[Log in to view URL]'))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💎IWV HUB💎",
   Callback = function()
   loadstring(game:HttpGet('https://[Log in to view URL]'))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💎Jay HUB💎",
   Callback = function()
   loadstring(game:HttpGet('https://[Log in to view URL]'))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💎FE trolling HUB💎",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💎Tiger x hub💎",
   Callback = function()
   loadstring(game:HttpGet("https://[Log in to view URL]"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "💎Universal Hub💎",
   Callback = function()
   loadstring(game:HttpGet('https://[Log in to view URL]'))()
   end,
})
